# PostgreSQL Database Configuration (usar application.yml y variables de entorno)
# spring.datasource.url=jdbc:postgresql://localhost:5432/gohost
# spring.datasource.driverClassName=org.postgresql.Driver
# spring.datasource.username=postgres
# spring.datasource.password=Admin1234

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway Configuration
# Habilitado para aplicar migraciones ubicadas en classpath:db/migration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Server Configuration
# Usa PORT cuando esté disponible (Railway), sino SERVER_PORT o 8080
server.port=${PORT:${SERVER_PORT:8080}}

# JWT Configuration
jwt.secret=bXlTZWNyZXRLZXlGb3JKV1RUb2tlbkdlbmVyYXRpb25BbmRWYWxpZGF0aW9uUHVycG9zZXM=
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:4200,http://localhost:3000
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS,HEAD
cors.allowed-headers=Authorization,Content-Type,Accept,Origin,X-Requested-With,X-CSRF-Token
cors.allow-credentials=true
cors.max-age=3600

# ============================================================
# MAIL CONFIGURATION - Production Ready
# ============================================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER:gohostapp1508@gmail.com}
spring.mail.password=${MAIL_PASS:aheshkwotsqqgooo}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
mail.from=${MAIL_FROM:gohostapp1508@gmail.com}
mail.from-name=${MAIL_FROM_NAME:GoHost}

# ============================================================
# LOGGING CONFIGURATION
# ============================================================
logging.level.root=INFO
logging.level.co.edu.uniquindio.gohost=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=INFO

# ============================================================
# GEOCODING CONFIGURATION (Nominatim / OpenStreetMap)
# ============================================================
geocoding.enabled=true
geocoding.provider=nominatim
geocoding.base-url=https://nominatim.openstreetmap.org/search
geocoding.api-key=
geocoding.user-agent=GoHost/1.0 (contacto: soporte@gohost.com)
geocoding.referer=https://gohost.local
geocoding.language=es
geocoding.limit=1
geocoding.timeout-ms=4000
geocoding.retries=1

# Recordatorios Configuration
app.recordatorios.horas-antes-checkin=24
app.recordatorios.horas-dia-checkin=2
app.recordatorios.max-intentos=3
app.recordatorios.enabled=true

# Scheduling Configuration
spring.task.scheduling.pool.size=5

# ============================================================
# CLOUDINARY CONFIGURATION (Cloud Image Service)
# ============================================================
nube.imagenes.habilitado=${CLOUDINARY_ENABLED:true}
nube.imagenes.nombre-nube=${CLOUDINARY_CLOUD_NAME:dnlsgjpbz}
nube.imagenes.clave-api=${CLOUDINARY_API_KEY:575249431351638}
nube.imagenes.clave-secreta=${CLOUDINARY_API_SECRET:wL4xW8u4K2qlhfsLKnujAiysMX0}
nube.imagenes.carpeta=${CLOUDINARY_FOLDER:gohost/imagenes}
nube.imagenes.tamano-maximo=${CLOUDINARY_MAX_SIZE:5242880}

# ============================================================
# SWAGGER/OPENAPI CONFIGURATION
# ============================================================
springdoc.swagger-ui.url=/openapi.yaml
springdoc.swagger-ui.path=/swagger-ui/index.html

# ============================================================
# MULTIPART CONFIGURATION (File Upload Limits)
# ============================================================
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ============================================================
# ACTUATOR CONFIGURATION (Health and Monitoring)
# ============================================================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
